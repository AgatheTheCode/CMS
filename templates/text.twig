{% extends "base.twig" %}
{% for element in page %}
{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <{{element.balise}} class ="{{element.class}}" alt="{{element.alt}}" id="{e{lement.id_css}} role="{{element.role}}">{{ element.content }}</{{element.balise}}>
                {% block link %}
                {% endblock %}
            </div>
        </div>
    </div>
        {% endfor %}
{% endblock %}